# 🚀 快速部署 tablet-version 到 GitHub Pages

## 最简单的方法（3步完成）

### 第1步：提交文件到 GitHub

```bash
# 添加 tablet-version 目录
git add tablet-version/

# 提交
git commit -m "Add tablet-optimized version"

# 推送
git push origin main
```

### 第2步：在 GitHub 网页上设置

1. 打开你的 GitHub 仓库
2. 点击 **Settings**（设置）标签
3. 在左侧菜单找到 **Pages**（页面）
4. 在 **Source**（源）部分：
   - 选择 **"Deploy from a branch"**
   - **Branch（分支）**：选择 `main`（或你的默认分支）
   - **Folder（文件夹）**：**选择 `/tablet-version`** ⬅️ **这是关键！**
5. 点击 **Save**（保存）

### 第3步：等待并访问

- 等待 1-2 分钟让 GitHub 部署
- 访问你的网站：`https://[username].github.io/[repository-name]`
- 现在应该显示平板优化版本了！

---

## 📸 设置截图说明

在 GitHub Pages 设置中，你会看到：

```
Source
┌─────────────────────────────────────┐
│ Deploy from a branch                │
│                                      │
│ Branch: [main ▼]                    │
│ Folder: [/ (root) ▼]                │
│         └─ /tablet-version  ← 选这个！│
│                                      │
│ [Save]                               │
└─────────────────────────────────────┘
```

**重要**：在 Folder 下拉菜单中选择 `/tablet-version`，而不是 `/ (root)`。

---

## ✅ 验证

部署成功后：
- 网站 URL：`https://[username].github.io/[repository-name]`
- 应该显示平板优化版本的调查问卷
- 所有选项都应该可见
- 表单可以正常提交

---

## 🔄 如果想切换回原始版本

只需在 GitHub Pages 设置中：
- 将 Folder 改回 `/ (root)`
- 点击 Save

---

## ⚠️ 常见问题

**Q: 部署后还是看到旧版本？**
- A: 等待 2-3 分钟，清除浏览器缓存，或使用无痕模式访问

**Q: 找不到 `/tablet-version` 选项？**
- A: 确保 `tablet-version/` 目录已提交并推送到 GitHub

**Q: 图片不显示？**
- A: 检查 `tablet-version/images/` 文件夹是否已上传到 GitHub

