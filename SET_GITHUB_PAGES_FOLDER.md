# 🎯 如何设置 GitHub Pages 使用 tablet-version 文件夹

## 问题
GitHub Pages 仍然显示主文件夹的 `index.html`，而不是 `tablet-version/index.html`。

## ✅ 解决方案（必须按步骤操作）

### 步骤 1：访问 GitHub 仓库设置

1. 打开你的 GitHub 仓库网页
2. 点击顶部的 **Settings**（设置）标签

### 步骤 2：找到 Pages 设置

1. 在左侧菜单中，向下滚动找到 **Pages**（页面）
2. 点击进入 Pages 设置页面

### 步骤 3：配置 Source（源）

在 **Source**（源）部分，你需要设置：

1. **选择 "Deploy from a branch"**
   - 不要选择 "Deploy from a branch" 的其他选项
   - 确保选择的是 "Deploy from a branch"

2. **Branch（分支）**：
   - 选择你的默认分支（通常是 `main` 或 `master`）

3. **Folder（文件夹）**：⬅️ **这是关键！**
   - 点击下拉菜单
   - **必须选择 `/tablet-version`**
   - **不要选择 `/ (root)`**

### 步骤 4：保存

1. 点击 **Save**（保存）按钮
2. 等待 GitHub 显示绿色提示："Your site is live at..."

### 步骤 5：等待部署

- GitHub Pages 通常需要 **1-2 分钟**来部署
- 有时可能需要 **5 分钟**
- 部署完成后，访问你的网站应该显示平板版本

---

## 📸 设置截图说明

在 GitHub Pages 设置页面，你应该看到：

```
┌─────────────────────────────────────────┐
│ Source                                   │
│ ┌─────────────────────────────────────┐ │
│ │ ○ None                              │ │
│ │ ● Deploy from a branch  ← 选这个！  │ │
│ │ ○ GitHub Actions                    │ │
│ └─────────────────────────────────────┘ │
│                                          │
│ Branch: [main ▼]                        │
│                                          │
│ Folder: [/ (root) ▼]                   │
│          ├─ / (root)                    │
│          └─ /tablet-version  ← 选这个！│
│                                          │
│ [Save]                                   │
└─────────────────────────────────────────┘
```

**重要**：Folder 必须选择 `/tablet-version`！

---

## 🔍 验证设置是否正确

设置完成后，检查：

1. **访问你的网站**：`https://[username].github.io/[repository-name]`
2. **应该看到**：
   - ✅ 表格布局（不是 flexbox）
   - ✅ 28px 大小的表单输入
   - ✅ 所有选项都可见
   - ✅ 页面标题包含 "(Tablet Version)"

3. **如果还是看到主版本**：
   - 清除浏览器缓存（Ctrl+Shift+Delete）
   - 使用无痕模式访问
   - 等待更长时间（最多 5 分钟）
   - 检查设置是否真的保存了

---

## 🆘 如果设置后还是不行

### 方法 A：使用重定向页面

我已经创建了 `index-redirect.html`，你可以：

1. **备份当前的 `index.html`**（如果需要保留）
2. **将 `index-redirect.html` 重命名为 `index.html`**
3. **提交更改**：
   ```bash
   git add index.html
   git commit -m "Redirect to tablet-version"
   git push origin main
   ```
4. **在 GitHub Pages 设置中选择 `/ (root)`**
5. 这样访问根 URL 会自动跳转到 `tablet-version/`

### 方法 B：将 tablet-version 内容复制到根目录

如果想用平板版本完全替换：

```bash
# 备份当前版本（可选）
cp index.html index-backup.html

# 复制平板版本到根目录
cp tablet-version/index.html index.html
cp -r tablet-version/images images

# 提交
git add index.html images/
git commit -m "Replace with tablet-optimized version"
git push origin main
```

然后在 GitHub Pages 设置中选择 `/ (root)`。

---

## ✅ 推荐方案

**我强烈推荐使用方法 1**（在 GitHub Pages 设置中选择 `/tablet-version` 文件夹），因为：
- ✅ 最简单
- ✅ 保留原始版本
- ✅ 可以随时切换
- ✅ 不需要修改代码

如果方法 1 不行，再使用方法 A（重定向）。

